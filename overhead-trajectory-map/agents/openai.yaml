interface:
  display_name: "场景关键帧与轨迹"
  short_description: "生成含完整人物与道具的起幅、中间关键帧和落幅 PNG"
  default_prompt: "使用 $overhead-trajectory-map，根据场景、人物和道具参考图生成含完整人物与物件的起幅、中间关键帧和落幅 PNG，检查形体与连续性；仅在我明确要求时绘制调度图。"
